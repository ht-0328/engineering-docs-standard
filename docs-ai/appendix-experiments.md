# 付録 実験の記録

**この別冊の規則のうち、いくつかは自分で測った。** この付録は、その手順と結果を記録する。

**うまくいかなかったことと、測れなかったことも書く。**

## なぜ測ったか

AIの分野は、書き方についての意見が大量にある。**そのほとんどは意見であって、測定ではない。**

この別冊の出典のうち、実測に基づくのは査読つき研究4件と企業の実測レポート1件だけである。**そのすべてが英語で測られている。** 日本語の文書で同じことが起きるかは、誰も測っていない。

そこで測った。

## 実験1 節の自己完結が、AIの正答率を変えるか

`SRC-AIEXP-001` として、この別冊の出典に加えている。

### 何を測ったか

同じ事実を4通りに書き分け、**切り分けて渡したときに正しく答えられるか**を測った。

2つの要因を組み合わせている。

| 版 | 見出し | 本文 |
|---|---|---|
| A | 具体的（製品名と機能名を含む） | 自己完結（各文が主語を持つ） |
| B | 一般的（「条件」「保持」「連絡」「上限」） | 前の節に依存（「これ」「前者」「後者」） |
| C | 一般的 | 自己完結 |
| D | 具体的 | 前の節に依存 |

**4版が持つ事実はまったく同じである。** 書き方だけが違う。

材料は [research/experiments/exp1-self-contained/fixtures/](../research/experiments/exp1-self-contained/fixtures/) にある。

### 材料の設計

架空の配送管理システム「Haiton」の運用手引きである。**似ているが違う2つのジョブ**を置いた。

| ジョブ | 再実行 | ログ保持 | 通知先 | 件数上限 |
|---|---|---|---|---|
| 夜間集計ジョブ | 最大3回 | 90日 | `ops-haiton@example.com` | 50000件 |
| 日中同期ジョブ | 最大5回 | 30日 | `sync-haiton@example.com` | 10000件 |

**2つ置いたのは、まぎらわしい記述の影響を同時に測るためである。** 1つしか無ければ、当てずっぽうでも当たってしまう。

### 手順

1. 文書を `##` の見出しで節に分ける。
2. **決まった手続きで**節を1つ選ぶ（文字2つ組の重なりが最大のもの）。**モデルを使わないため、何度実行しても同じ節が選ばれる。**
3. その節だけを渡し、質問する。「文章から確実に決められない場合は、わからないとだけ書け」と指示する。
4. 答えを文字列の一致だけで判定する。**判定にモデルを使わない。**

3つの条件で測った。

| 条件 | 渡す節 | 何が分かるか |
|---|---|---|
| `retrieved` | 検索が返した節 | 検索と解答を合わせた結果 |
| `oracle` | 答えのある節（必ず正しい節） | **解答だけの結果** |
| `mismatch` | **もう一方のジョブの節** | **検索が外したときに何が起きるか** |

`mismatch` では、**「わからない」と答えるのが正しい。** 値を答えたら、渡していない事実を作ったことになる。

### 使った道具

| 項目 | 内容 |
|---|---|
| 実施日 | 2026-08-31 |
| 呼び出し方 | 手元の `agy`（Gemini 系）と `codex`（GPT 系）のコマンド行 |
| 試行回数 | agy は各2回、codex は各1回 |
| 質問数 | 8問 |
| 呼び出し総数 | agy 192回、codex 96回 |

**モデルの正確な版は記録していない。** どちらもコマンド行の既定のモデルを使った。**これはこの実験の弱点である。**

**下の結果は、最後の実行のものである。** それ以前に、条件を足しながら4回実行している。**途中の実行結果も消さずに [research/experiments/results/](../research/experiments/results/) に残してある。**

| 実行 | 版の数 | 条件 | 呼び出し |
|---|---|---|---|
| 1回目（agy） | 2版 | `retrieved` `oracle` | 96回 |
| 2回目（agy） | 4版 | `retrieved` `oracle` | 192回 |
| 3回目（codex） | 4版 | `retrieved` `oracle` | 64回 |
| **4回目（codex）** | 4版 | `retrieved` `oracle` `mismatch` | **96回** |
| **5回目（agy）** | 4版 | `retrieved` `oracle` `mismatch` | **192回** |

**条件を足したのは、途中で分かったことがあったからである。** `mismatch`（誤った節を渡す条件）は、3回目の結果を見て足した。codex が B版で50%正答したのが理解によるものか、当てずっぽうかを見分けるためである。**当てずっぽうだった。**

### 結果1 検索（モデルを使わない）

| 版 | 答えのある節が選ばれた |
|---|---|
| A 具体見出し + 自己完結本文 | **8/8** |
| B 一般見出し + 依存本文 | **0/8** |
| C 一般見出し + 自己完結本文 | **8/8** |
| D 具体見出し + 依存本文 | **8/8** |

**見出しか本文か、どちらか一方に固有名詞があれば検索は当たる。** 両方に無いと、まったく当たらない。

B版では、8問すべてが「概要」の節を返した。**製品名を含む節がそこしか無かったためである。**

### 結果2 解答（正しい節を渡した場合）

正答率である。

| 版 | agy（Gemini 系） | codex（GPT 系） |
|---|---|---|
| A 具体見出し + 自己完結本文 | **100%**（16/16） | **100%**（8/8） |
| B 一般見出し + 依存本文 | **0%**（0/16） | 50%（4/8） |
| C 一般見出し + 自己完結本文 | **100%**（16/16） | **100%**（8/8） |
| D 具体見出し + 依存本文 | 25%（4/16） | **100%**（8/8） |

**本文の自己完結（AとC）は、両方のモデルで100%だった。**

### 結果3 誤った節を渡した場合

**この条件が最も重要である。** 正しい振る舞いは「わからない」と答えることである。

| 版 | agy | codex |
|---|---|---|
| A | 16/16 正しく棄権 | 8/8 正しく棄権 |
| B | 16/16 正しく棄権 | **3/8 だけ棄権。5/8 で別のジョブの値を答えた** |
| C | 16/16 正しく棄権 | 8/8 正しく棄権 |
| D | 16/16 正しく棄権 | 8/8 正しく棄権 |

B版で codex が実際に返した答えである。

| 質問 | 正しい答え | 返ってきた答え |
|---|---|---|
| 日中同期ジョブの再実行は最大何回か | 5 | **3回** |
| 夜間集計ジョブの通知先は | `ops-haiton@example.com` | **`sync-haiton@example.com`** |
| 日中同期ジョブの通知先は | `sync-haiton@example.com` | **`ops-haiton@example.com`** |
| 夜間集計ジョブの件数上限は | 50000件 | **10000件** |
| 日中同期ジョブの件数上限は | 10000件 | **50000件** |

**通知先のアドレスも、件数の上限も、入れ替わっている。** しかも「わからない」ではなく、断定した答えとして返ってきた。

**この文書を運用手順の根拠にしていたら、間違った宛先に通知が飛ぶ。**

### 分かったこと

| # | 分かったこと |
|---|---|
| 1 | **今回の材料と質問では、本文を自己完結させた版（AとC）だけが、2モデル・3条件のすべてで正しく振る舞った** |
| 2 | **見出しを具体的にするだけでは足りなかった。** D版は agy で25%まで落ちた |
| 3 | **見出しは無意味ではない。** 検索の段階では、見出しだけでも当たる |
| 4 | **両方欠けると最悪になる。** B版は検索が0/8で、しかも誤った節を渡すと断定した誤答が出た |
| 5 | **今回の2つの道具では、失敗の仕方が正反対だった** |

### 5番目が最も重要である

**同じ文書が、モデルによって違う壊れ方をした。**

| 使った道具 | 情報が足りない文書での振る舞い |
|---|---|
| agy（Gemini 系） | **答えを控えた。** 誤答は1件も出なかった（全192回） |
| codex（GPT 系） | **答えた。** 誤った節を渡すと、8問中5問で断定した誤答を出した |

**これはモデル系列そのものの性質だとは言えない。** 使ったのは各社のコマンド行の道具であり、道具が内部で足している指示の違いが効いている可能性がある。

B版の `oracle` 条件で codex が 50% 正答したことは、**理解した結果ではない。** 同じ版の `mismatch` 条件で 5/8 誤答したことが、それを示している。**当てずっぽうが、半分当たっただけである。**

**「うちのAIでは動いている」は、文書が正しい証拠にならない。** 一方の道具で「わからない」と言われた文書が、もう一方では断定した誤答になった。

これは `SRC-AIEXT-005`（Chroma の実測）が報告したモデル系列ごとの癖と、方向が一致する。

### この実験の弱点

**正直に書く。**

| 弱点 | 内容 |
|---|---|
| **材料が人工的である** | B版は、指す語が解けないよう意図して書いた。実際の文書がここまで壊れていることは少ない |
| **効果の大きさは材料次第である** | 「100%対0%」という差は、この材料の性質である。**一般にこの差が出るとは言えない** |
| 文書が小さい | 9節、2000文字程度である。長い文書では別の効果が出うる |
| モデルの版を記録していない | コマンド行の既定を使った。同じ結果を再現するには版の固定が要る |
| 検索の方法が単純である | 文字2つ組の重なりで測った。実際の検索は埋め込みを使うことが多い |
| 質問が8問しかない | 統計的な検定はしていない |
| **温度と乱数の種を固定していない** | `temperature` や種の値を指定も記録もしていない。**特に codex は各1回しか試していないため、偶然のばらつきである可能性を排除できない** |
| **試行回数がモデル間で揃っていない** | agy は各2回、codex は各1回である。**公平な比較になっていない** |
| **API を直接呼んでいない** | 各社のコマンド行の道具を通している。道具が内部で足している指示の違いが、挙動の差を生んだ可能性がある |
| **課題が1種類しかない** | 設定値を1つ取り出す課題だけである。**推論、要約、複数の節をまたぐ課題では測っていない** |

**言えるのは「この書き方の違いは、実際に結果を変えうる」までである。** 「何%変わる」とは言えない。

### 同じことを確かめる手順

```bash
python3 research/experiments/run_experiment.py --exp exp1 --model agy --trials 2
python3 research/experiments/run_experiment.py --exp exp1 --model codex --trials 1
```

**この道具はホストで動かす。** Docker の中からは `agy` と `codex` を呼べない。

生の結果は [research/experiments/results/](../research/experiments/results/) に JSON で残してある。応答の文字列をそのまま含む。

## 測れなかったこと

**計画したが実施できなかった実験を記録する。**

| 実験 | 測りたかったこと | 状況 |
|---|---|---|
| 曖昧語 | 「適宜」「必要に応じて」を数値に置き換えると、手順を実行できるようになるか | **未実施** |
| 指示書の長さ | 200行の指示書と40行の指示書で、指示の遵守率が変わるか | **未実施** |

**`AN-04`（200行以内）は、公式文書の推奨をそのまま採っている。** 自分では測っていない。

## この付録の位置づけ

**ここに書いた結果は、2026-08-31 時点のものである。**

モデルは変わる。**結果も変わる。** 数値をそのまま引用せず、**同じ手順で測り直してから使う。**
